package group4.graphics;

import group4.game.GameState;

/**
 * Renderer class which is used to take in a GameState to produce a render!
 */
public class Renderer {
    // Private so nobody makes an instance
    private Renderer() {

    }

    /**
     * Renders to the screen given all elements in the given GameState.
     * @param gs GameState, contains all information regarding the current state space of the game
     */
    public static void render(GameState gs) {

    }
}
