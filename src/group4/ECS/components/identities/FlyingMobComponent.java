package group4.ECS.components.identities;

import com.badlogic.ashley.core.Component;

/**
 * FlyingMobComponent, needed if the object needs to be registered as an Entity for the flying mob movement system.
 */
public class FlyingMobComponent implements Component {
    /**
     * Default constructor which creates the component. No data is currently associated with being a flying mob.
     */
    public FlyingMobComponent() {
    }

}
