package group4.ECS.components.identities;

import com.badlogic.ashley.core.Component;

/**
 * WalkingMobComponent, needed if the object needs to be registered as an Entity for the walking mob movement system.
 */
public class WalkingMobComponent implements Component {
    /**
     * Default constructor which creates the component. No data is currently associated with being a walking mob.
     */
    public WalkingMobComponent() {
    }

}
