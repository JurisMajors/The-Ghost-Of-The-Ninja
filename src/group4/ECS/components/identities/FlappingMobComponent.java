package group4.ECS.components.identities;

import com.badlogic.ashley.core.Component;

/**
 * FlappingMobComponent, needed if the object needs to be registered as an Entity for the flapping mob movement system.
 */
public class FlappingMobComponent implements Component {
    /**
     * Default constructor which creates the component. No data is currently associated with being a flapping mob.
     */
    public FlappingMobComponent() {
    }

}
